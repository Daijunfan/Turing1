# MORPH-SAT 项目真实性与价值潜力验证报告

验证日期：2026-08-30  
验证对象：`morphosat_research_bundle.zip` 及其解压项目  
验证环境：macOS 26.3.2、Apple M5 Pro、arm64、Python 3.14.6

## 一、最终结论

本项目不是空壳、伪代码或只含自述材料的包装项目。压缩包中的核心算法、实验数据、SAT 模型、UNSAT 证书和独立检查入口均真实存在；在本次验证覆盖的范围内，主要正确性结果可以复现，最大四个随包证据也全部通过重新求解或独立重放。因此，可以确认：**MORPH-SAT 是一个机制真实、结果可检查、具有明确研究想法和继续发展价值的非玩具研究原型。**

但现有材料不能支持“已经具备图灵奖级成果”或“已经完成公认原创突破”的结论。项目没有作者、机构、许可证、代码历史、公开仓库、论文、DOI、同行评审或第三方复现实验；主要性能证据来自项目自己生成的合成问题，尚未经过主流 SAT 基准和同机现代专用求解器对照。其底层构件——精确变量消元、join-project、Schaefer 可解类、多态性、GF(2)、2-SAT、Horn 求解和证明检查——均有深厚既有研究基础。当前最可能成立的原创点是这些构件形成的运行时闭环，而不是任何单一构件。

综合判定如下：

| 待验证事项 | 判定 | 证据边界 |
|---|---|---|
| 压缩包与随包文件完整性 | 已验证 | ZIP CRC、SHA-256、清单逐项通过 |
| 核心算法确有实现 | 已验证 | 审查 2,248 行核心 Python，而非仅依据文档 |
| 随包 SAT/UNSAT 旗舰结果 | 已验证 | 四个旗舰均在本机重新通过 |
| 小规模正确性声明 | 已复现 | 5,000 例得到完全相同的 760/760 决定性正确结果 |
| 原始结果表内部一致性 | 已验证 | 汇总、CSV、拟合参数重新计算一致 |
| 结果由所附生成器产生 | UNSAT 旗舰已验证 | 两个 UNSAT 旗舰的规范化 CNF 哈希与生成器输出精确匹配 |
| 性能具有普遍优势 | 未验证 | 仅有合成族和 Z3 单一限时基线 |
| 学术原创性 | 尚不能确认 | 未发现同名公开成果，但无系统性查新或同行评审 |
| 作者身份与权属 | 无法验证 | 包内完全缺少作者、机构、许可证和版本历史 |
| 图灵奖级别 | 尚未达到 | 缺少基础定理、广泛影响、独立采用与长期学术验证 |

## 二、验证范围与方法

本次没有把项目自带的 `verified: true` 当作结论，而是分别检查了四层证据：

1. 文件层：压缩包结构、CRC、SHA-256、项目清单和文件哈希。
2. 实现层：DIMACS 解析、关系恢复、Schaefer 分类、精确融合、后端求解、模型重建和证书检查代码。
3. 运行层：编译、单元测试、穷举对拍、随机后端测试、四个旗舰重求解、模型检查、证书重放和篡改拒绝。
4. 外部层：与 Schaefer 理论、bucket elimination、SAT backdoor、现代 SAT 结构检测、证明检查规范和 SAT Competition 要求比较，并检索公开同名成果。

“已验证”只表示结论落在上述证据覆盖范围内，不等于证明算法对任意 CNF 都高效，也不等于证明项目具有法律意义上的原创权属。

## 三、真实性与可复现性证据

### 3.1 压缩包和文件完整性

- 原始压缩包 SHA-256：`9127a3b3477366163c602eab9f958cf3786b53e10078251a3fdcebbc134bbeed`。
- ZIP 共 82 个条目，其中 75 个文件、7 个目录；未压缩内容 21,768,353 字节。
- `unzip -t` 检查全部条目均为 `OK`。
- `MANIFEST.sha256` 覆盖除清单自身外的 74 个文件，逐项校验全部通过。
- 随包源代码、原始 CSV/JSON、CNF、模型、证书和图表之间没有发现哈希或数量冲突。

这证明当前压缩包内部没有传输损坏，也证明报告所引用的结果文件与打包时登记的文件一致。它不能单独证明作者身份或实验是在所称环境中完成的。

### 3.2 核心实现不是占位代码

核心包共有 2,248 行 Python，实际实现了以下完整链路：

- 按子句变量域恢复有界局部真值表关系；
- 通过语义枚举判定 affine、bijunctive、Horn、dual-Horn、0-valid 和 1-valid；
- 执行精确的 `join + existential projection`，并保存融合 DAG；
- 使用 GF(2) 高斯消元、2-SAT SCC、Horn/dual-Horn 前向链求解；
- 对 SAT 结果逆序寻找被消元变量的见证，并重新检查原始 CNF；
- 对 UNSAT 结果从原始 CNF 重建关系、重放融合、验证蕴含方程，最终检查 XOR 是否得到 `0=1`；
- 在无法形成支持语言时返回 `UNKNOWN`，而不是猜测答案。

代码审查确认精确融合满足：

\[
R'(B)=\exists x\;\bigwedge_i R_i(B_i,x),
\]

因此每一步保持剩余变量上的投影解集。该正确性基础是成立的。其代价也很明确：真值表枚举和投影成本随宏关系元数指数增长，所以“宽度有界”是算法能够运行的关键前提。

### 3.3 本机重新运行结果

本次对四个最大随包实例既重新调用求解器，也分别调用模型/证书检查器：

| 实例 | 规模 | 本机重新求解 | 独立产物检查 |
|---|---:|---:|---|
| direct affine UNSAT | 28,672 变量 / 229,376 子句 | `UNSAT`，已验证，3.940 秒 | 13,286 条局部方程，最终 `0=1` |
| direct affine SAT | 28,672 / 229,376 | `SAT`，已验证，4.073 秒 | 229,376 个原始子句全部满足 |
| gate fusion UNSAT | 1,824 / 4,416 | `UNSAT`，已验证，5.445 秒 | 重放 1,679 次融合、50 条方程，最终 `0=1` |
| gate fusion SAT | 1,824 / 4,416 | `SAT`，已验证，5.413 秒 | 逆向重建后 4,416 个原始子句全部满足 |

本机时间用于确认结果确实可以快速重现，不应与包内 Linux 环境时间直接做硬件性能比较。

对两个最大 UNSAT 证书分别篡改首条方程或首个融合步骤后，检查器均拒绝接受。直接证书检查逻辑是声音的：每条被选方程都必须由原始局部关系逐元组蕴含，所有方程异或后必须得到零系数和右端 1。融合证书额外要求每个父关系仍处于活动状态，并从父真值表重新计算投影关系，不能仅凭证书自述通过。

### 3.4 小规模正确性和后端稳健性

本次重新执行了报告所用的 5,000 个固定种子六变量 CNF，并使用单独编写的穷举循环计算真值：

- 总实例：5,000；
- MORPH-SAT 给出决定性答案：760；
- 与穷举一致：760/760；
- 内部产物验证通过：760/760；
- 错误答案：0；
- `UNKNOWN`：4,240。

该结果与随包报告逐项相同。另对随机生成的 2,000 个 2-SAT 和 2,000 个 Horn 实例进行了穷举对拍，两类后端均为 0 个错误状态、0 个错误 SAT 模型。对 300 个小型随机 CNF 的融合路径也未发现错误决定性答案。

### 3.5 原始实验表与汇总一致

不使用项目汇总函数，直接读取 CSV 并重新计算，得到：

- 四个隐藏语言家族：142/142 正确、142/142 验证、142/142 与随包记录的决定性 Z3 结果一致；
- 跨语言融合：36/36 正确并验证，且无融合版本全部为 `UNKNOWN`；
- 随机 3-SAT 负对照：200/200 在直接和融合模式均为 `UNKNOWN`；随包 Z3 记录为 129 SAT、71 UNSAT；
- direct scaling 拟合：指数 `1.0080303706833011`，`R²=0.9998622511555825`；
- fusion scaling 拟合：指数 `1.7831434522709224`，`R²=0.9980050802879731`；
- 轨迹消融：固定轨迹成功 37/40，四轨迹成功 39/40，完整策略恢复最后 1 例；
- 宽度阈值表在宏元数 2–5 不形成共同 affine，在 6–8 形成，原始行与汇总一致。

这些检查证明汇总数字不是由 CSV 中不存在的数据拼出来的。幂律指数仍只是该合成族和测试区间上的经验拟合，不能解释为渐近复杂度定理。

### 3.6 旗舰文件与生成器的来源一致性

两个最大 UNSAT 旗舰可以从所附生成器得到相同的规范化 CNF 哈希：

- `generate_obfuscated_tseitin(8192, private_per_vertex=2, unsat=True, seed=6008192)` 产生 `a46a11b1...eaa7f59`，与 direct 证书中的 CNF 哈希一致；
- `generate_gate_obfuscated_tseitin(192, unsat=True, seed=4001920, encoding="compact")` 产生 `0a3ab43e...a7da81a`，与 fusion 证书中的 CNF 哈希一致。

这显著加强了“CNF、生成器、证书属于同一实验链”的可信度。缺点是 `REPRODUCE.md` 没有写出这些种子，也没有提供生成四个 8,192/192 旗舰及 SAT 模型的统一脚本；SAT 旗舰虽有有效模型和清单哈希，但暂时不能按文档字节级再生成。

### 3.7 测试和复现缺陷

源代码编译检查通过。11 个随包单元测试中，本机有 10 个通过，唯一失败项是 `Z3FFI` 把动态库路径硬编码为 Linux 的 `/lib/x86_64-linux-gnu/libz3.so.4`，在 macOS 上直接抛出 `OSError`。包内保存的 Linux 运行记录显示该项曾通过，但这只是随包记录，不是本机独立结果。

完整实验脚本还依赖 pandas、Matplotlib 和特定系统 Z3；依赖虽写在 `requirements.txt`，Z3 路径却没有平台发现或配置入口。因此当前“完整一条命令复现”仅对接近原记录的 Linux 环境成立，可移植性不足。

## 四、技术含金量分析

### 4.1 真正有价值的部分

项目最有含金量的不是“发现了 XOR”或“使用了变量消元”，而是一个完整、可证伪的运行时闭环：

\[
\text{无标签 CNF}
\rightarrow \text{局部语义关系}
\rightarrow \text{有界精确融合}
\rightarrow \text{共同多态性检测}
\rightarrow \text{专用精确后端}
\rightarrow \text{模型或可重放证明}.
\]

其中三点尤其值得保留：

1. **表示发现与求解耦合。** 系统不是只做一次静态分类，而是在消元过程中持续重新分类残余关系，并在共同语言出现时切换算法。
2. **精确、不猜测。** 融合是有穷真值表上的精确投影；没有发现结构时返回 `UNKNOWN`。这使原型适合成为可插拔的专用预处理器或组合求解器组件。
3. **证据链完整。** SAT 结果回到原 CNF 逐子句检查；UNSAT 结果能够从原输入重放局部推导。证明产出是高可信求解系统的重要方向，LRAT 工作也说明了“复杂求解器 + 简单可信检查器”的学术与工程价值（[Efficient Certified RAT Verification](https://arxiv.org/abs/1612.02353)）。

### 4.2 理论位置是合理的，但基础操作并不新

Schaefer 早已证明广义布尔可满足性语言要么多项式可解、要么 NP 完全（[The Complexity of Satisfiability Problems](https://doi.org/10.1145/800133.804350)）。现代 CSP 理论以多态性刻画语言，并把“合取 + 存在量化”视为标准 primitive-positive 定义（[Polymorphisms, and How to Use Them](https://andreikrokhin.webspace.durham.ac.uk/wp-content/uploads/sites/78/2021/04/polym_survey.pdf)）。

同样，项目的 join-project 是经典变量/桶消元。Dechter 的 bucket elimination 框架已经统一了 SAT、CSP、线性系统和动态规划，并指出复杂度对 induced width 指数依赖（[Bucket Elimination: A Unifying Framework for Reasoning](https://ics.uci.edu/~csp/r48b.pdf)）。把实例移入可解类的思想也与 SAT/CSP backdoor 研究邻近；既有工作已经研究隐藏可解结构、backdoor treewidth 和多个“可解岛”的组合（[Backdoors to Typical Case Complexity](https://www.cs.cornell.edu/gomes/pdf/2003_williams_ijcai_backdoors.pdf)、[Combining Treewidth and Backdoors for CSP](https://drops.dagstuhl.de/entities/document/10.4230/LIPIcs.STACS.2017.36)、[Backdoor Treewidth for SAT](https://ac.tuwien.ac.at/files/tr/ac-tr-17-014.pdf)）。

因此，“形态宽度”是否是新的、严格更强的参数，必须通过与 induced width、treewidth、backdoor size/depth/treewidth 和证明宽度的正式关系或分离定理来确认。现有代码只给出了轨迹相关的实验上界，尚未完成这个理论责任。

### 4.3 候选原创贡献

截至验证日，以 `MORPH-SAT`、`MORPH-CSP`、`Morphogenetic Polymorphism Compilation`、`Morphogenetic Polymorphism Compiler` 和 `Emergent Tractability as a Computational Resource` 为精确词组检索 arXiv、GitHub 仓库和普通网页，没有找到与本包相同的公开论文或实现。这个结果说明项目不像是从一个同名公开仓库直接复制而来，但**检索无结果不能证明绝对原创**。

在当前证据下，最稳妥的候选原创表述是：

> 在有界精确消元过程中，把残余约束集合的共同多态性当作运行时状态；当异质关系经多代投影形成共同 Schaefer 语言时，立即编译到对应精确后端，并为整个转换与求解过程生成可重放证据。

要把这句话从“有意思的系统组合”提升为“原创理论贡献”，仍需正式查新、清晰定义、非平凡定理、与最近邻参数的分离，以及同行评审。

## 五、限制真实性结论和含金量上限的关键事实

### 5.1 主要基准由项目自身构造

direct affine 基准把每个局部 affine 关系用全部禁用元组编码为同变量域宽子句。虽然变量、极性和子句顺序被打乱，但相同变量域天然暴露了局部块边界，求解器按相同 scope 聚合即可恢复关系。它证明了语义恢复和 GF(2) 管线，却不是最强的“无边界隐藏结构”证据。

gate fusion 基准更有说服力：XOR 被拆成 AND/OR/NOT/常量的紧凑 CNF，子句 scope 不再直接等于完整门边界，初始关系也没有共同 affine 类，必须经过数百至上千次融合才形成 affine 残余语言。不过，它仍是由同一项目按已知目标语言专门构造的 Tseitin 家族，不能代替外部真实问题。

### 5.2 基线不足以证明领先

项目只记录了 Z3 4.13.3、2 秒上限的对照，而且较大规模不再运行 Z3。现代专用求解器本身已经具备结构恢复能力：CaDiCaL 的公开版本说明包含 AND/XOR/ITE gate 检测、BVA 和语义定义提取（[CaDiCaL releases](https://github.com/arminbiere/cadical/releases)）；CryptoMiniSat 明确支持 XOR 与 Gauss-Jordan 消元（[CryptoMiniSat](https://github.com/msoos/cryptominisat)）。在没有同机 CaDiCaL、Kissat、CryptoMiniSat 等对照前，不能把现有 Z3 超时解释为 SAT 求解性能领先。

### 5.3 尚未具备通用 SAT Competition 能力

SAT Competition 2026 主赛道使用 300–600 个基准，要求 SAT 模型和 UNSAT 证书，并以 5,000 秒、32GB 和 PAR-2 排名（[competition tracks](https://satcompetition.github.io/2026/tracks.html)）；规则还要求公开源代码、作者、机构、系统说明和错误产物零容忍（[competition rules](https://satcompetition.github.io/2026/rules.html)）。

本次从官方 2026 主赛道列表抽取 `arles_thres20_p10_r7533.cnf` 测试。该实例有 2,320 个变量、8,620 个子句，最大原始子句宽度 79；MORPH-SAT 的直接和融合模式都因局部 scope 超过 8 而立即抛出 `ValueError`。这符合原型自己声明的有界 scope 设计，但证明它目前不能作为通用竞赛求解器提交。官方基准还明确要求求解器处理未清洗的重复字面量和重言子句（[benchmark requirements](https://satcompetition.github.io/2026/benchmarks.html)），本项目尚无相应竞赛级验证。

### 5.4 学术来源和权属无法核验

项目内没有以下任何信息：

- 作者姓名、机构、邮箱或 ORCID；
- `LICENSE`、版权声明或第三方许可证清单；
- Git 历史、版本标签、发布页或可验证时间戳；
- 论文、预印本、DOI、引用文件或投稿信息；
- 人工与 AI 生成代码比例说明。

所以可以验证“这份代码运行出了这些可检查结果”，但不能验证“是谁、何时、以什么研究过程创造了它”，也不能为原创权、可再分发权或学术署名背书。

### 5.5 证明检查器尚非形式化可信核

随包检查器比求解器小且逻辑清晰，篡改测试有效，当前证书在数学上有直接可核验含义；但检查器与求解器共享 CNF、关系和融合实现，并没有在 Coq、Isabelle、ACL2、Lean 或 CakeML 中形式化验证。相较 LRAT/CakePB 等经过形式化验证的生态，它应称为“独立可执行检查器”，不应称为“形式化认证证明系统”。

## 六、价值潜力

### 6.1 近期最现实的价值：可证明的结构编译器

最可行的产品/系统位置不是替代完整 CDCL 求解器，而是作为前置结构编译器：输入普通 CNF，在有界预算内发现 affine、2-SAT、Horn 或 dual-Horn 残余；成功时交给专用后端并输出证明，失败时把控制权交回通用求解器。这种“成功有证据、失败无副作用”的接口边界清楚，也符合项目当前返回 `UNKNOWN` 的设计。

若与 IPASIR/CaDiCaL/CryptoMiniSat 集成，并能在真实硬件验证、密码分析、规划或配置实例中找到现有预处理器漏掉的结构，该能力具有直接工程价值。

### 6.2 中期学术价值：连接结构参数与代数参数

树宽描述交互结构，Schaefer/多态性描述关系语言，backdoor 描述到可解类的距离。项目提出的核心问题是：能否在只付出小中间宽度的情况下，通过精确消元使实例进入统一代数语言，并提前停止通用消元？如果“形态宽度”能被严格定义、可近似计算，并被证明与现有参数不可约，它有机会形成一个新的参数化复杂性研究方向。

真正高价值的理论结果至少应包含一种自然实例族，使形态宽度保持常数或低阶，而 treewidth、经典 backdoor 或某类证明宽度必须增长；同时还要有多项式时间算法找到成功轨迹。没有这种定理，“新参数”仍可能只是经典消元宽度与提前停止条件的重新命名。

### 6.3 长期潜力：运行时发现问题语言

项目的更大愿景是让系统先发现当前实例服从的代数，再生成匹配算法。若同一机制能在不依赖人工标签的情况下扩展到有限域线性约束、差分约束、图可达性、流或程序不变量，并保持可重放证据，它将从一个 SAT 技巧上升为通用的“表示发现—算法合成”框架。

这一方向概念上重要，因为它试图统一结构识别、知识编译、算法选择和可信计算。但当前实现只验证了布尔 Schaefer 类和合成 CNF，跨领域价值仍属于研究假设。

### 6.4 价值兑现所需的决定性证据

要把现有潜力变成公认高含金量成果，最关键的不是继续扩大同类合成实例，而是补足四类证据：

1. 一篇把定义、正确性、复杂度和最近邻工作讲清楚的公开论文，并经过同行评审。
2. 一组来自 SAT Competition 和真实应用、事先未按目标语言构造的盲测结果，与当前 CaDiCaL、Kissat、CryptoMiniSat 等同机比较。
3. 一个严格分离定理或不可替代的实证优势，证明该闭环不是普通 BVE/bucket elimination/backdoor 检测的等价包装。
4. 完整研究溯源：作者与机构、许可证、公开版本历史、固定环境、全量生成脚本、种子和标准证明格式。

## 七、图灵奖级别判断

图灵奖评价的是已经对计算领域产生持久、重大影响的贡献，而不是尚有潜力的单次原型。按这一标准，本项目目前缺少三个决定性条件：

- **基础性结果**：尚无新的复杂性定理、完备算法或对现有理论的严格分离；
- **外部确认**：尚无同行评审、独立复现、学术引用、竞赛结果或实际部署；
- **持续影响**：项目刚形成单个研究包，尚不能证明长期改变研究或工程实践。

因此最终判断是：

> **真实性方面，核心机制和主要有限实验结果可信；含金量方面，达到“有清晰研究问题、可运行、可证伪、规模非玩具的优秀早期原型”层级；原创性和普适性能尚未得到外部确认；现阶段不能认定为图灵奖级成果。其最高潜力取决于能否把运行时多态性形成从工程组合提升为严格的新理论资源，并在真实基准上表现出不可由现有方法替代的优势。**
